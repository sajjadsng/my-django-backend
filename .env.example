# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (for local development)
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DATABASE_URL=

# CORS Settings
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=

# Port (Fly.io will set this automatically)
PORT=8000

# Fly.io will provide these values in production:
# - DATABASE_URL (PostgreSQL)
# - PORT
# - SECRET_KEY (should be set manually)
# - DEBUG=False
# - ALLOWED_HOSTS=.fly.dev 